<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/al_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/al_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Backlogin"
        tools:targetApi="31">
        <activity
            android:name=".CarroComprasActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".PinActivity" />
        <activity android:name=".ReciboActivity"/>

        <activity android:name=".DB.InicioActivity"/>
        <activity android:name=".PagoActivity"/>
    </application>

</manifest>